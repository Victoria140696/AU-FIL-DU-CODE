{% extends 'base.html.twig' %}

{% block title %}Edition
{% endblock %}

{% block body %}
	<h1 class="d-flex justify-content-center m-5">Edition de site internet</h1>

	<div class="d-flex flex-row justify-content-center mb-2">


		{{ include('admin_website/_form.html.twig', {'button_label': 'Update'}) }}

	</div>

	<div class="d-flex justify-content-center">
		<a class="btn btn-info" href="{{ path('app_admin_website_index') }}">Retour</a>
	</div>

	{{ include('admin_website/_delete_form.html.twig') }}
{% endblock %}
